# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# Env / secrets — never commit
.env*
!.env.example
.mcp.json

# Node
node_modules/
dist/

# Data files with real lead/client info (back up separately)
data/leads/raw/
data/leads/cleaned/
data/clients/
data/conversations/inbox/

# OS
.DS_Store
Thumbs.db
